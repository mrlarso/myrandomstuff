
class List:
    def count_spec_digits(self, int_list, dig_list):
        allints = []
        for number in int_list:
            for unit in list(str(number))
        for dig in dig_list:
            for int in int_list:
                for unit in
